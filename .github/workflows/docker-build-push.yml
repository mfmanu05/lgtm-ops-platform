name: Build and Push Images

on:
  push:
    branches:
      - main


jobs:

  build:

    runs-on: ubuntu-latest


    steps:

      - name: Checkout repository
        uses: actions/checkout@v4



      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}



      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Verify DockerHub username
          run: |
          echo "DockerHub user: ${{ secrets.DOCKERHUB_USERNAME }}"

      - name: Build and Push Backend
        uses: docker/build-push-action@v6
        with:
          context: ./app/backend
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/lgtm-backend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/lgtm-backend:${{ github.sha }}



      - name: Build and Push Frontend
        uses: docker/build-push-action@v6
        with:
          context: ./app/frontend
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/lgtm-frontend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/lgtm-frontend:${{ github.sha }}
